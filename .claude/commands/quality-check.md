# /quality-check

5ゲート品質チェックを実行する（デプロイせずにチェックのみ）。

## 手順
1. `deploy-checker` スキルを実行
2. 結果をテーブル形式で報告
3. FAIL 項目がある場合は修正方針を提案

## /deploy との違い
- `/quality-check` はチェックのみ。デプロイ手順は案内しない
- `/deploy` はチェック + デプロイ手順案内
